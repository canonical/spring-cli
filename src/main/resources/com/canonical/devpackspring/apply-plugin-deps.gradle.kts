initscript {
    repositories {
        // configure the list of repositories
        %s
    }
    dependencies {
        // configure the list of classpath dependencies
        classpath("%s")
    }
}
